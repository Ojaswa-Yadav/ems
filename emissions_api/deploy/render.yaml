services:
  - type: web
    name: emissions-api
    env: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    autoDeploy: true
    region: oregon
    plan: starter
    healthCheckPath: /health